apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deploy-ctrl.fullname" . }}-test-connection"
  labels:
    {{- include "deploy-ctrl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deploy-ctrl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
